#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    // int a = 4;
    // char c = '8';
    // cout << "The value of a is: " << a << " and value of c is :" << c << endl;
    // a = 8;
    // c = 'a';
    // cout << "The value of a is: " << a << " and value of c is :" << c << endl;

    // Constant
    //  const int a=4;
    //  cout<<"THE value of a is :"<<a<<endl;
    //  a=8; //Constant value are not change able,if we change it gives error

    // iomanip
    // int a = 5, b = 1256, c = 256;
    // cout << "The value of a is: " << a << endl;
    // cout << "The value of b is: " << b << endl;
    // cout << "The value of c is: " << c << endl;
    // // Manipulating values

    // cout << "The value of a is: " << setw(4) << a << endl;
    // cout << "The value of b is: " << setw(4) << b << endl;
    // cout << "The value of c is: " << setw(4) << c << endl;

    // Operator precedence
    // int a = 5, b = 6, c = 3;

    // cout << "The value of ((((a*b)+c)-a)+b) is: " << ((((a * b) + c) - a) + b);

    return 0;
}